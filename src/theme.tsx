
// theme.ts
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  typography: {
    fontFamily: 'Ubuntu mono, monospace',
  },
  // You can also customize other typography settings here if needed
});

export default theme;
